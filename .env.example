# Port the server listens on (Render/Railway inject this automatically)
PORT=4000

# Set to true ONLY for local testing against localhost APIs.
# In production keep it false so the SSRF guard stays active.
ALLOW_PRIVATE_HOSTS=false
